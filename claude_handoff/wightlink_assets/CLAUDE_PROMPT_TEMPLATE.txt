You are preparing a Wightlink {{quarter_short}} PPC report deck.

V2 note
If the upload includes UPDATED_SLIDE_MAPPING_WIGHTLINK_QBR_V2_TEMPLATE.csv and QA_CHECKLIST_V2_ADDENDUM.txt, follow those V2 files. The V2 deck remains quarter-led overall. Trend slides compare current YTD vs previous YTD, KPI cards include Plan lines where available, Red Funnel is a same-quarter YoY comparison when prior-year auction data is supplied, and the performance section is intentionally lean.

I am uploading:
- report.txt
- {{streamlit_pptx_filename}}
- reference_deck_exported_from_google_slides.pptx, if included
- original_streamlit_prompt.txt
- SLIDE_MAPPING.csv
- SOURCE_SECTION_INDEX.txt
- REFERENCE_DECK_OUTLINE.txt
- QA_CHECKLIST.txt
- CHART_QA_ADDENDUM_FOR_CLAUDE.txt
- INPUT_FILES_MANIFEST.txt
- PACKAGE_MANIFEST.json
- source_data/ raw CSV files
- UPDATED_SLIDE_MAPPING_WIGHTLINK_QBR_V2_TEMPLATE.csv, if included
- QA_CHECKLIST_V2_ADDENDUM.txt, if included
- V2 rule files, if included

Target reference deck:
{{reference_deck_url}}

Task
Create a new Wightlink {{quarter_short}} report deck that is visually and structurally aligned to the Wightlink reference deck, but populated with {{period_label}} data from report.txt.

Final deliverable requirement
The final client deliverable must be suitable for Google Slides. If you have native Google Slides editing, copy the reference Google Slides deck first and edit the copy. If you do not have native Google Slides editing, use reference_deck_exported_from_google_slides.pptx as the intermediate editing file, return the finished PPTX, and make sure it can be imported back into Google Slides without changing the visual system.

Source priority
1. report.txt is the source of truth for all numbers, tables, bullets, KPI values, dates, and chart content.
2. SLIDE_MAPPING.csv defines how source sections map to the target slides.
3. The Google Slides reference deck and reference_deck_exported_from_google_slides.pptx define the final visual style, slide order, and layout language.
4. source_data/ CSVs are audit/backup sources and can be used for chart regeneration if report.txt does not expose enough chart detail.
5. {{streamlit_pptx_filename}} is only a secondary aid for understanding coverage. Do not use its styling as the target.
6. original_streamlit_prompt.txt is background only.

Hard requirements
- Do not edit the original reference deck.
- Replace all old period labels/content with {{period_label}} content.
- Preserve the Wightlink reference deck design: dark navy title/section slides, Wightlink accent colors, KPI cards, tables, footers, typography, chart placement, and spacing.
- Use report.txt values exactly. Do not recalculate or round differently unless resolving a source inconsistency.
- Do not invent Red Funnel, ad creative, testing, next steps, other updates, or top-campaign content if it is not in report.txt.
- Every report.txt source section must be represented.
- Convert missing reference sections into clearly marked human-review placeholders.
- If V2 files are present, use the lean V2 structure in SLIDE_MAPPING.csv. Do not recreate removed Plan vs Actual detail slides or the old Purchases+CPA / Revenue+ROAS chart-pair slides.
- Red Funnel slide must show Red Funnel only. If source_data/auction_insights_red_funnel_prior_year_quarter.csv is supplied, include the YoY pp change columns.
- Campaign-type chart slides must use one monthly purchases and purchase revenue chart only.
- Apply CHART_QA_ADDENDUM_FOR_CLAUDE.txt to every chart slide. Charts must not be squashed, clipped, or overlapping; inspect full-slide screenshots/thumbnails after chart replacement.
- Run QA_CHECKLIST.txt before returning.

Output
Return the new copied Google Slides deck URL if you edited Google Slides directly. If you used the PPTX bridge, return the finished PPTX only and note that it should be imported into Google Slides. Also list any placeholders that remain for manual completion.
